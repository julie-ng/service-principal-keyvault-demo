# Get secret value from terraform
terraform output demo_secret

# Compare with value in Key Vault
az keyvault secret show \
	--name demo-secret \
	--vault $(terraform output key_vault_name | tr -d '"') | jq '.value'
